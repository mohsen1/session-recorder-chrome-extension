import { defineBackground } from 'wxt/sandbox';

// Placeholder — replaced by the full session orchestrator during the build.
export default defineBackground(() => {
  console.log('[session-recorder] background booted');
});
